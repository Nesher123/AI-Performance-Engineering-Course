# Nebius Token Factory API Configuration
NEBIUS_API_KEY=<your-api-key>

# Optional: raises Hugging Face download rate limits for SWE-bench datasets/images.
HF_TOKEN=

# Optional: override MLflow backend. Empty uses sqlite:///mlflow.db.
MLFLOW_TRACKING_URI=