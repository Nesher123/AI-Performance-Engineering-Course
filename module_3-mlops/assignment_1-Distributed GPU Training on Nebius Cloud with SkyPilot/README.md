Full task description: https://gitlab.com/jadnov/nebius-academy-ddp

In this assignment you'll set up a full distributed training pipeline on Nebius Cloud - build a Docker image, provision a Kubernetes cluster with GPU nodes, and run a multi-node DDP training job across two GPUs on separate nodes using SkyPilot. Submit a .zip archive with exactly these files:

mk8s-ng-config.json Dockerfile train.py train_job.yaml training_log.txt